export interface MissionCard {
  id: number;
  title: string;
  description: string;
  imageUrl: string; // URL of the mission card image
  backgroundColor: string; // Background color for the mission card
}

export interface MissionDetail {
  id: number;
  title: string;
  description: string;
  imageUrl: string; // URL of the mission card image
  backgroundColor: string; // Background color for the mission card
}